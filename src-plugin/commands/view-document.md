---
description: Open Trio wiki in the browser, rooted at the current working directory. Auto-invoke when the user asks to open their Markdown wiki, browse notes, or view the current folder as a wiki.
---

Run `"${CLAUDE_PLUGIN_ROOT}/skill/launch.sh" "$PWD"` and report the URL + PID it prints. If the script prints `already running`, tell the user the existing browser tab was reused.
